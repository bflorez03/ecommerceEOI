import { TotalCostePipe } from './total-coste.pipe';

describe('TotalCostePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalCostePipe();
    expect(pipe).toBeTruthy();
  });
});
